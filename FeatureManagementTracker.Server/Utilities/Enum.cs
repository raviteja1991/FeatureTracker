namespace FeatureManagementTracker.Server.Utilities
{
    public class Enum
    {
        public enum Complexity
        {
            S, M, L, XL
        }

        public enum Status
        {
            New, Active, Closed, Abandoned
        }
    }
}
